import DefaultLayout from "./components/DefaultLayout";

const Home: React.FC = () => {
  return (
    <DefaultLayout top={0} right={0} bottom={0} left={0}>
      <div>hi</div>
    </DefaultLayout>
  );
};

export default Home;
